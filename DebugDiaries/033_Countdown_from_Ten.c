// Write a C program that uses a for loop to print the numbers from 10 down to 1.
// Each number should be on a new line.
// ----------------------------------------------------------------------------------------
#include <stdio.h>

int main() {

    for (int i =10; i>0; i-- ){
        printf("%d\n", i);
    }
    
    return 0;
}
